export default function Loading() {
  return (
    <main className="marketing-page" style={{ display: "grid", placeItems: "center" }}>
      <p style={{ color: "var(--muted)", fontSize: "1.2rem" }}>Loading...</p>
    </main>
  );
}
